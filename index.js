import React from 'react';
import ReactDOM from 'react-dom/client';
import TradingViewStyleDashboard from './TradingViewStyleDashboard';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TradingViewStyleDashboard />
  </React.StrictMode>
);